/*Write a C program to find the prime factors of a given number and display them.*/

#include <stdio.h>

int main(void)
{
    int num;

    do
    {
        printf("Enter number: ");
        scanf("%d", &num);
    } while (num <= 0);
    
}